namespace Belp.Build.Test.MSBuild.Loggers;

/// <summary>
/// Represents a method that will handle a diagnostic event.
/// </summary>
/// <param name="diagnostic"></param>
public delegate void DiagnosticEventHandler(Diagnostic diagnostic);
